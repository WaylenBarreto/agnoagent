# Set these values in PowerShell or load them with your environment manager. Never commit secrets.
OLLAMA_API_KEY=replace-with-your-ollama-api-key
DATABASE_URL=postgresql+psycopg://rag:rag_password@localhost:5432/rag
# Comma-separated api-key:company-id entries. Send the key in the X-API-Key header.
RAG_API_KEYS=dev-api-key:company-1
